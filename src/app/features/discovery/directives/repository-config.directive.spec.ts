import { RepositoryConfigDirective } from './repository-config.directive';

describe('RepositoryConfigDirective', () => {
  it('should create an instance', () => {
    const directive = new RepositoryConfigDirective();
    expect(directive).toBeTruthy();
  });
});
