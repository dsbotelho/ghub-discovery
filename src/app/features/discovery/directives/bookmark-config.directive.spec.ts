import { BookmarkConfigDirective } from './bookmark-config.directive';

describe('BookmarkConfigDirective', () => {
  it('should create an instance', () => {
    const directive = new BookmarkConfigDirective();
    expect(directive).toBeTruthy();
  });
});
